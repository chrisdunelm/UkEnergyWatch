package org.ukenergywatch.www

import scalatags.Text.all._

class EletricSummaryView {
/*
  // This class is transfered from server -> client
  case class Data(
    aNumber: Int
  )

  def aNumber(data: Data): Tag = {
    span(data.aNumber)
  }

  def update(data: Data): Unit = {
    val numberTag = aNumber(data)
    dom.getElementById(Ids.myNumber).replace(numberTag)
    val wordTag = aWord(data)
    dom.getElementById(Ids.myWord).replace(wordTag)
  }
*/
}
